@{
  Layout = "_MainLayout";
}
@model InterfaceRepository
<body>
  <div id="content">
    <h2>Ärenden</h2>
    <p class="info">Du är inloggad som avdelningschef</p>

    <table id="managerForm">
      <tr>
        <td class="label">Välj status:</td>
        <td class="label">Välj handläggare:</td>
        <td>&nbsp;</td>
        <td class="label">Ärendenummer:</td>
        <td>&nbsp;</td>
      </tr>
      <tr>
        <td>
          <select name="status">
            <option selected="selected">Välj alla</option>
            @foreach (ErrandStatus errandstatus in Model.ErrandStatuses) {
              <option value="stat">@errandstatus.StatusName.</option>
            }
          </select>
        </td>
        <td>
          <select name="investigator">
            <option selected="selected">Välj alla</option>
            @foreach (Employee employee in @ViewBag.ListOfInvestigators) {

              <option value="stat">@employee.EmployeeName.</option>
            }
          </select>
        </td>
        <td><input class="button" type="submit" value="Hämta lista" /></td>
        <td><input name="casenumber" /></td>
        <td><input class="button" type="submit" value="Sök" /></td>
      </tr>
    </table>
    @await Component.InvokeAsync("ErrandList")
  </div>
  
</body>
